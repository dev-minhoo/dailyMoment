<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.cNerds.dailyMoment.user.UserInfo">
	
	<!-- ============================================================= -->
	<!-- resultMap                                                     -->
	<!-- ============================================================= -->
	<resultMap type="com.cNerds.dailyMoment.user.UserInfo" id="com.cNerds.dailyMoment.user.UserInfo.resultMap">
		<id property="userNo"				column="USER_NO"/>
		<result property="userId"			column="USER_ID" />
		<result property="userPwd"			column="USER_PWD" />
		<result property="userName"			column="USER_NAME" />
		<result property="userNickName"			column="USER_NICK_NAME" />
		<result property="isAgree"			column="IS_AGREE" />
		<result property="isSocial"			column="IS_SOCIAL" />
		<result property="userAuthToken"			column="USER_AUTH_TOKEN" />
		<result property="isDelete"			column="IS_DELETE" />
		<result property="createOn" 					 column="CREATE_ON"/>
		<result property="creator.userNo" 					 column="CREATOR"/>
		<result property="modifiedOn" 					 column="MODIFIED_ON"/>
		<result property="modifier.userNo" 					 column="MODIFIER"/>
	</resultMap>
	
	<!-- ============================================================= -->
	<!-- Query Fragment                                                -->
	<!-- ============================================================= -->
	<sql id="columns">
		UI.USER_NO, UI.USER_ID, UI.USER_PWD, UI.USER_NAME, UI.USER_NICK_NAME,
		UI.IS_AGREE, UI.IS_SOCIAL, UI.USER_AUTH_TOKEN, UI.IS_DELETE,
		UI.CREATE_ON, UI.CREATOR, UI.MODIFIED_ON, UI.MODIFIER
  	    
	</sql>
	<sql id="tables">
		T_USER_INFO UI
	</sql>
	<sql id="keyCriterion">
		UI.USER_NO = #{userNo}
	</sql>
	
	<sql id="searchCriterion">
			
	</sql>
	<sql id="searchOrder">
		UI.USER_NO DESC
	</sql>
	
	<!-- ============================================================= -->
	<!-- Exist                                                         -->
	<!-- ============================================================= -->
	<select id="exist" parameterType="com.cNerds.dailyMoment.user.UserInfo" resultType="int">
	</select>
	
	<!-- ============================================================= -->
	<!-- Search                                                        -->
	<!-- ============================================================= -->
	
	<select id="list" parameterType="com.cNerds.dailyMoment.user.UserInfoCriterion" resultMap="com.cNerds.dailyMoment.user.UserInfo.resultMap">
		SELECT <include refid="columns"/>
		FROM <include refid="tables"/>
		<where>
			<include refid="searchCriterion"/>
		</where>
		ORDER BY <include refid="searchOrder"/>		
	</select>
	
	
	<select id="listCount" parameterType="com.cNerds.dailyMoment.user.UserInfoCriterion" resultType="long">
		SELECT COUNT(*)
		FROM <include refid="tables"/>
		<where>
			<include refid="searchCriterion"/>
		</where>
	</select>
	
	<select id="detail" parameterType="com.cNerds.dailyMoment.user.UserInfo" resultMap="com.cNerds.dailyMoment.user.UserInfo.resultMap">
		SELECT <include refid="columns"/>
		FROM <include refid="tables"/>
		<where>
			<include refid="keyCriterionRe"/>
		</where>
	</select>

	<!-- ============================================================= -->
	<!-- Insert / Update / Delete                                      -->
	<!-- ============================================================= -->
	<insert id="insert" parameterType="com.cNerds.dailyMoment.user.UserInfo">
		<selectKey keyProperty="userNo" resultType="Integer" order="BEFORE"> 
			SELECT IFNULL(MAX(USER_NO), 0)+1 userNo FROM T_USER_INFO 
		</selectKey>
		 
		INSERT INTO T_MEMBER_INFO
		(
			USER_NO
			, USER_ID
			, USER_PWD
			, USER_NAME
			, USER_NICK_NAME
			, IS_AGREE
			, IS_SOCIAL
			, IS_DELETE
			, CREATE_ON
			, CREATOR
						
			
		)
		VALUES
		(
			#{userNo}			
			, #{userId}
			, #{userPwd}
			, #{userName}
			, #{userNickName}
			, #{isAgree}
			, #{isSocial}
			, #{isDelete}
			, NOW()			
			, #{userNo}					
		)
	</insert>
		
	<update id="update" parameterType="com.cNerds.dailyMoment.user.UserInfo">
		UPDATE 
			T_USER_INFO UI
		SET
			USER_ID					=			#{userId}
			USER_PWD				=			#{userPwd}
			USER_NAME				=			#{userName}
			USER_NICK_NAME			=			#{userNickName}
			IS_AGREE				=			#{isAgree}
			IS_SOCIAL				=			#{isSocial}
			USER_AUTH_TOKEN			=			#{userAuthToken}
			IS_DELETE				=			#{isDelete}
			MODIFIED_ON				=			NOW()
			MODIFIER				=			#{modifier.userNo}
				
		<where>
		 <include refid="keyCriterionRe"/>
		</where>
	</update>
	
	<delete id="delete" parameterType="com.cNerds.dailyMoment.user.UserInfo">
		DELETE FROM MI USING T_USER_INFO MI
			<where>
		 	<include refid="keyCriterion"/>
		 </where>
	</delete>
	
	
	
	
	
	
</mapper>